# Trimmed down from the original 130+ line file, which was emergent.sh's
# default project template — it pulled in heavy AI/ML SDKs (google-genai,
# openai, litellm, tiktoken, huggingface_hub, grpcio, ...) and dev tooling
# (black, mypy, flake8, pytest, ...) that this backend's code never actually
# imports. Those unused packages were also exact-pinned against a different
# Python version than most real hosts run, which is what caused the
# grpcio-status/google-api-core conflict on Render's Python 3.14.
#
# Below is only what backend/server.py and backend/lesson_reminders.py
# actually import, with compatible-release ranges (~=) instead of exact
# pins, so pip can resolve versions that actually work on the host's
# Python version instead of failing on a mismatch.

fastapi~=0.110
uvicorn[standard]~=0.25
starlette~=0.37
pydantic~=2.13
email-validator~=2.3

python-dotenv~=1.2
python-jose[cryptography]~=3.5
passlib[bcrypt]~=1.7
bcrypt~=4.1

motor~=3.3
pymongo~=4.5

httpx~=0.28
stripe~=13.0
APScheduler~=3.11

# Receipt OCR (25 Aug 2026) — replaces the old emergentintegrations/Gemini
# call, which stopped working once Grant moved off the emergent.sh platform.
anthropic~=1.1

# Testing (used by backend/tests/ — harmless in production, but only
# actually needed for `pytest` to run; kept for parity with local dev)
pytest~=9.0
requests~=2.33
